import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'polylogicapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
